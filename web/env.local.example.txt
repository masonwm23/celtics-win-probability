# Where the Python API is listening. Only needed if you change the port or run
# the API on another machine. The default below is what scripts/21_serve_api.py
# uses, so in the normal case you do not need this file at all.
NEXT_PUBLIC_API_BASE=http://127.0.0.1:8000
